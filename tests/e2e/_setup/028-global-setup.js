/**
 * @file No-op global setup for feature-scoped Strava OAuth E2E.
 * @returns {void}
 */
export default function globalSetup() {
  // Feature 028 only needs a setup entrypoint for E2E_FEATURE scoping.
}
