/**
 * @file EventDeleteConfirm Component (Stub — TDD RED phase)
 * @description Placeholder for the delete confirmation dialog.
 * Will be implemented after tests are verified as RED.
 */

/**
 * @typedef {object} EventDeleteConfirmProps
 * @property {string} eventId - 要刪除的活動 ID。
 * @property {(id: string) => void} onConfirm - 確認刪除的回呼。
 * @property {() => void} onCancel - 取消刪除的回呼。
 * @property {boolean} [isDeleting] - 是否正在刪除中。
 * @property {string} [deleteError] - 刪除失敗時的錯誤訊息。
 */

/**
 * EventDeleteConfirm — stub for TDD.
 * @param {EventDeleteConfirmProps} _props - Component props.
 * @returns {null} Nothing rendered yet.
 */
export default function EventDeleteConfirm(_props) {
  return null;
}
