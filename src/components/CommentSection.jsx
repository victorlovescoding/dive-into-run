/**
 * @file CommentSection — 活動留言區容器元件。
 * @description TDD stub — 元件已宣告但尚未實作。
 */

/**
 * 留言區容器，管理所有留言相關 state。
 * @param {object} props - 元件 props。
 * @param {string} props.eventId - 活動 ID。
 * @returns {import('react').ReactElement} Rendered component.
 */
export default function CommentSection({ eventId }) {
  void eventId;
  return null;
}
