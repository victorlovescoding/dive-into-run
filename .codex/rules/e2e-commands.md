# E2E Commands

E2E tests are temporarily removed for the testless reset.

All E2E npm scripts currently print a disabled message and exit successfully.
Do not use Playwright commands as verification evidence until a replacement
suite is introduced.
