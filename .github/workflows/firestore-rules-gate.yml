name: Firestore Rules Gate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  firestore-rules-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Firestore rules gate placeholder
        run: echo "Firestore rules gate placeholder is intentionally inactive."
