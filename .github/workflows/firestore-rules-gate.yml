name: Firestore Rules Gate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  firestore-rules-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Firestore rules test gate disabled
        run: echo "Firestore rules test gate disabled for testless reset."
